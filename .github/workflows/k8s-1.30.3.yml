name: GitGoogleContainer
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v2
      - name: Login in Docker Registry
        uses: docker/login-action@v2.2.0
        with:
          registry: registry.cn-hangzhou.aliyuncs.com
          username: l240250076
          password: Aisino@2024
      - name: Docker pull and push
        run: |

          #docker pull --platform linux/arm64 nacos/nacos-server:v2.5.1
          docker pull swr.cn-north-4.myhuaweicloud.com/ddn-k8s/ghcr.io/ggml-org/llama.cpp:server

          #docker tag nacos/nacos-server:v2.5.1 registry.cn-hangzhou.aliyuncs.com/opennamespace/nacos-server:v2.5.1-arm
          docker tag swr.cn-north-4.myhuaweicloud.com/ddn-k8s/ghcr.io/ggml-org/llama.cpp:server registry.cn-hangzhou.aliyuncs.com/opennamespace/llama.cpp:server

          #docker push registry.cn-hangzhou.aliyuncs.com/opennamespace/nacos-server:v2.5.1-arm
          docker push registry.cn-hangzhou.aliyuncs.com/opennamespace/llama.cpp:server



          

      
