name: GitGoogleContainer
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v2
      - name: Login in Docker Registry
        uses: docker/login-action@v2.2.0
        with:
          registry: registry.cn-hangzhou.aliyuncs.com
          username: l240250076
          password: Aisino@2024
      - name: Docker pull and push
        run: |
          docker pull registry.k8s.io/kube-proxy:v1.32.0
          docker pull registry.k8s.io/coredns/coredns:v1.11.3

          docker tag registry.k8s.io/kube-proxy:v1.32.0   registry.cn-hangzhou.aliyuncs.com/opennamespace/kube-proxy:v1.32.0
          docker tag registry.k8s.io/coredns/coredns:v1.11.3   registry.cn-hangzhou.aliyuncs.com/opennamespace/coredns:v1.11.3
          
          docker push registry.cn-hangzhou.aliyuncs.com/opennamespace/kube-proxy:v1.32.0
          docker push registry.cn-hangzhou.aliyuncs.com/opennamespace/coredns:v1.11.3

          

      
